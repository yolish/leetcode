class Solution(object):
    def getRow(self, rowIndex):
        """
        :type rowIndex: int
        :rtype: List[int]
        """

        n = rowIndex + 1



        i + 1 % 2 == 0
        len = i + 1
        1, i - 1, (i - 1) * 3, i - 1 * 5...index == i / 2, +(i - 1 * 2), 1